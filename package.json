{
  "name": "inspectctl",
  "version": "0.0.1",
  "description": "MCP server skeleton (stdio). CDP tools live on the http branch.",
  "type": "module",
  "scripts": {
    "start": "node src/index.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  }
}
